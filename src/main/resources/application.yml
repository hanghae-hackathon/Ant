spring:
  application:
    name: Ant

thymeleaf:
  cache: false
  check-template-location: true
  prefix: classpath:/templates/
  suffix: .html
  enabled: true

file:
  path: /home/ec2-user/file/

server:
  port: 8088

openai:
  api-key: ${OPENAI_API_KEY}